import tailwindConfig from "@com/ui/config";
import { Meta, Typeset } from "@storybook/blocks";
import resolveConfig from "tailwindcss/resolveConfig";

<Meta title="Design System/Typography" />

export const fullConfig = resolveConfig(tailwindConfig);
export const SampleText = "The quick brown fox jumps over the lazy dog";

# Typography

## Font Sizes

<Typeset
  fontSizes={Object.entries(fullConfig.theme.fontSize).map(([name, value]) =>
    Number(value[0].replace("rem", "") * 16),
  )}
  fontWeight={fullConfig.theme.fontWeight.normal}
  sampleText={SampleText}
  fontFamily={fullConfig.theme.fontFamily.sans.join(", ")}
/>

## Font Weights

{Object.entries(fullConfig.theme.fontWeight).map(([name, value]) => (

<div key={name}>
  <h3 className="capitalize">{name}</h3>
  <Typeset
    fontSizes={[
      Number(fullConfig.theme.fontSize["4xl"][0].replace("rem", "") * 16),
    ]}
    fontWeight={value}
    sampleText={SampleText}
    fontFamily={fullConfig.theme.fontFamily.sans.join(", ")}
  />
</div>
))}

## Font Families

### Sans

<Typeset
  fontSizes={[Number(fullConfig.theme.fontSize.xl[0].replace("rem", "") * 16)]}
  fontWeight={fullConfig.theme.fontWeight.normal}
  sampleText={SampleText}
  fontFamily={fullConfig.theme.fontFamily.sans.join(", ")}
/>

### Serif

<Typeset
  fontSizes={[Number(fullConfig.theme.fontSize.xl[0].replace("rem", "") * 16)]}
  fontWeight={fullConfig.theme.fontWeight.normal}
  sampleText={SampleText}
  fontFamily={fullConfig.theme.fontFamily.serif.join(", ")}
/>

### Mono

<Typeset
  fontSizes={[Number(fullConfig.theme.fontSize.xl[0].replace("rem", "") * 16)]}
  fontWeight={fullConfig.theme.fontWeight.normal}
  sampleText={SampleText}
  fontFamily={fullConfig.theme.fontFamily.mono.join(", ")}
/>
